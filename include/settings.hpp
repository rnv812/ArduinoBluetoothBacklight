#pragma once

// MESSAGES
#define PRINT_FEEDBACK              true        // wether to print feedback messages to serial port
#define PRINT_DEBUG                 false       // wether to print packet data to serial port (recommended to use it only in debug)
#define PRINT_STATE                 false       // wether to print strip state to serial port (recommended to use it only in debug)


